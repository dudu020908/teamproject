<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- 앱 전체 테마: Application 이 참조 -->
    <style name="Theme.Teamproject" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- 커스텀 항목 필요 시 추가 -->
    </style>
    <!-- 런치(스플래시) 테마: Activity 의 android:theme -->
    <style name="LaunchTheme" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@drawable/launch_background</item>
        <item name="android:forceDarkAllowed">false</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowDrawsSystemBarBackgrounds">false</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>
    <!-- Flutter UI가 뜬 뒤의 배경: meta-data NormalTheme -->
    <style name="NormalTheme" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@android:color/white</item>
    </style>
</resources>
